# Firmware documentation

@mainpage Firmware documentation

## Overview

Add a short description of the firmware, supported boards, sensors, communication buses, and safety constraints.

## Architecture

Document the main modules here:

- `src/` for application logic.
- `include/` for public project headers.
- `lib/` for reusable project libraries.
- `test/` for PlatformIO unit tests.

## Build and debug

Use the VS Code tasks included with this environment for building, static analysis, docs, and debugging.

